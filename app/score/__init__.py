"""Static score layout and rendering (implemented in a later milestone).

Contract: ::

    PlayableNote[] -> ScoreLayout -> LayoutElements -> Renderer

This layer must never touch audio, MIDI or transcription models.
"""
